name: Legacy Tag Release Stub

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-stub:
    runs-on: ubuntu-latest
    steps:
      - name: Stub notice
        run: echo "Legacy stub only. Use release-from-tag.yml for tagged releases."
